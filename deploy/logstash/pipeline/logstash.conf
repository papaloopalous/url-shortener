input {
  beats {
    port => 5044
  }
}

filter {
  if [container][name] =~ /shortener-service/ {
    json {
      source => "message"
      target => "log"
    }

    if [log][level] {
      mutate {
        rename => { "[log][level]" => "level" }
        rename => { "[log][msg]"   => "msg" }
        rename => { "[log][time]"  => "log_time" }
      }
    }

    mutate {
      add_field => { "service" => "shortener-service" }
    }

    mutate {
      remove_field => ["message", "log"]
    }

    date {
      match => ["log_time", "ISO8601"]
      target => "@timestamp"
      remove_field => ["log_time"]
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "shortener-service-%{+YYYY.MM.dd}"
  }
}