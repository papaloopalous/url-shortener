global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "shortener-service"
    static_configs:
      - targets: ["shortener-service:8082"]
    metrics_path: /metrics

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]